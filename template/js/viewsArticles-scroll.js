/*window.addEventListener('scroll', ArticlesIsLoaded);

function ArticlesIsLoaded() {*/

	//window.onscroll = function() {

		/*var sidebarId = document.getElementById('sidebar-id');

		var paginationPosition = document.getElementById('pagination-id');

		if(paginationPosition.getBoundingClientRect().top <= 900) {*/

			/*sidebarId.style.opacity = '0';
			sidebarId.style.transition = '0.2s';*/
			//sidebarId.style.top = '-500px';

		//}else{

			/*sidebarId.style.opacity = '1';
			sidebarId.style.transition = '1s';*/
			//sidebarId.style.top = '0';

		//}

	//}

//}